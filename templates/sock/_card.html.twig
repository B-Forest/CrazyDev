<div class='w-full'>
    <div class="inline-block max-w-xs rounded overflow-hidden shadow-lg h-auto bg-{{ socklonely.color.twclass }}-200">
        <div class="font-bold text-xl mb-2 justify-center items-center flex "
             id="name">
            <a href="{{ path('lost_sock',{'id' : socklonely.id}) }}">{{ socklonely.username }}</a>
        </div>
        <div class="justify-center items-center flex"><img class="w-3/6 bg-transparent"
                                                           src="{{ asset(socklonely.pattern.path) }}" alt="Motifs">
        </div>
        <div class="px-6 py-4 h-[14rem]">
            <h2 class="justify-center flex">Resumé</h2>
            <div class="h-[9rem] overflow-hidden text-clip">
                <p class="text-gray-700 text-base">
                    {{ socklonely.story }}
                </p>
            </div>
            <div class="text-center">
                <a href="{{ path('lost_sock',{'id' : socklonely.id}) }}">
                    <button class="rounded">Voir le profil</button>
                </a>
            </div>
        </div>
    </div>
</div>