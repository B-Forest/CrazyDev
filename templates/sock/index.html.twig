{% extends 'base.html.twig' %}

{% block title %}TTC - Chaussettes perdues{% endblock %}

{% block body %}
      {% include 'sock/_filter.html.twig'%}
            {% if (socks) %}
                <div class="grid grid-cols-3">
                {% for socklonely in socks %}
                    {% include 'sock/_card.html.twig' %}
                {% endfor %}
            {% else %}
                <div>
                    <p class="text-2xl text-center">Il n'y a aucune chaussettes perdues malheuresement..</p>
                </div>

            {% endif %}
        </div>
{% endblock %}
