<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        
    </head>
    <body>  
     <header class="flex justify-between mr-auto ml-auto mt-8 max-w-screen-xl mb-36">
        <nav class"">
            <ul class="flex text-base font-semibold gap-14 ml-24">
             <li><a class="underline text-pink-500" href="">Accueil</a></li>
             <li><a class="underline text-pink-500" href="{{path('app_sock')}}">Paire Trouvées</a></li>
             <li><a class="underline text-pink-500" href="{{ path('app_pair') }}">Pairs</a></li>
            </ul>
        </nav>

        <div class="flex">
        {% if app.user %}    
        <div class="border-2 border-pink-500 px-3 py-2 text-pink-500 font-semibold mr-24">             
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
        {% else %}
        <div class="border-2 border-pink-500 px-3 py-2 text-pink-500 font-semibold mr-10">  
            <a href="{{ path('app_login') }}">Connexion</a>
        </div>
        <div class="border-2 border-pink-500 px-3 py-2 text-pink-500 font-semibold mr-24">  
            <a href="{{ path('app_register') }}">Inscription</a>
        </div>
        {% endif %}
        </div>
        
        
     </header>
    
      
        {% block body %}{% endblock %}
        <footer class="mr-auto ml-auto max-w-screen-xl mb-8 mt-36">
            <p class="flex justify-center">
				<small>
					Copyrights 2023 - Chaussettes - <a class="underline" href="mentions_legales.html">Mentions légales</a>
				</small>
			</p>
        
        </footer>

    </body>
</html>
