{% extends 'base.html.twig' %}

{% block title %}TTC - Inscription{% endblock %}

{% block body %}
    <h1 class="text-3xl font-bold uppercase text-pink-500 flex justify-center max-w-screen-xl mb-20 mr-auto ml-auto">Register</h1>
    <section>
        <div class="flex flex-col justify-center items-center">
            {{ include('registration/_form.html.twig') }}
        </div>
        <div class="flex flex-col justify-center items-center">
            <div>
                <a class="underline  text-pink-500" href="{{ path('app_login') }}">J'ai déjà un compte ?</a>
            </div>
        </div>
    </section>
{% endblock %}
